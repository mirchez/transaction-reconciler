# Database URL (PostgreSQL)
# For Neon: postgresql://username:password@host.neon.tech/dbname?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/transaction_reconciler"

# Google OAuth Credentials (for Gmail integration)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# OpenAI API Key (for AI-powered transaction matching and PDF parsing)
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY="your_openai_api_key"

# Next.js Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production URLs (update for deployment)
# GOOGLE_REDIRECT_URI="https://yourdomain.com/api/auth/google/callback"
# NEXT_PUBLIC_APP_URL="https://yourdomain.com"